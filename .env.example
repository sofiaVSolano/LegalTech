# Archivo de ejemplo para variables de entorno
# Copia este archivo como .env y completa con tus valores reales

# Clave de API de OpenAI (requerida)
OPENAI_API_KEY=tu_clave_openai_aqui

# Clave secreta de Flask (opcional, se genera automáticamente si no se especifica)
SECRET_KEY=tu_clave_secreta_flask

# Modelo de OpenAI a usar (opcional, por defecto gpt-4-1106-preview)
GPT_MODEL=gpt-4-1106-preview

# Puerto de la aplicación (opcional, por defecto 5002)
PORT=5002

# Entorno de Flask (opcional, por defecto production)
FLASK_ENV=production